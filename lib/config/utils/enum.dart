enum ScreenType { mobile, tablet, desktop }

enum SectionType {
  header,
  about,
  tech,
  projects,
  experience,
  community,
  contact,
}

enum ProjectType { openSource, product, mobileApp, webApp }

enum TechCategory {
  mobile,
  web,
  server,
  database,
  vcs,
  design,
}
